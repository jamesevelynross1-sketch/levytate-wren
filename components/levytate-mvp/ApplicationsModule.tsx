"use client";

import { useState, type FormEvent } from "react";
import type { RequestStatus } from "@/lib/levytate/domain";
import { EmptyState, FormActions, FormField, FormGrid, FormSelect, FormTextArea, MvpModal, MvpPanel, MvpToolbar, StatusBadge, TableAction, TableBody, TableHead, TableShell } from "@/components/levytate-mvp/MvpUi";
import { useMvpWorkspace } from "@/components/levytate-mvp/MvpWorkspaceStore";
import { displayEmployee, includesSearch, statusTone } from "@/components/levytate-mvp/module-utils";
import { activeApplicationStatuses, createMvpId, nowIso, todayIso, type MvpApplication } from "@/lib/levytate/mvp/workspace";

const requestStatuses: RequestStatus[] = ["Draft", "Submitted to Line Manager", "Awaiting Manager Review", "Declined by Line Manager", "Approved by Line Manager", "Submitted to Apprenticeship Lead", "Awaiting Final Approval", "Declined by Apprenticeship Lead", "Approved for Enrolment", "Withdrawn", "Completed", "Cancelled"];

export function ApplicationsModule() {
  const { data, saveApplication, updateApplicationStatus } = useMvpWorkspace();
  const [search, setSearch] = useState("");
  const [status, setStatus] = useState("All");
  const [draft, setDraft] = useState<MvpApplication | null>(null);
  const [error, setError] = useState("");
  const visible = data.applications.filter((application) => { const employee = data.employees.find((item) => item.id === application.employeeId); return (status === "All" || application.status === status) && includesSearch([employee?.name, application.pathwayTitle, application.status], search); });

  function blank(): MvpApplication { return { id: createMvpId("application"), employeeId: "", pathwayTitle: "", status: "Draft", reason: "", careerGoal: "", managerNote: "", submittedAt: todayIso(), updatedAt: nowIso() }; }
  function pathwaysForEmployee(employeeId: string) { const employee = data.employees.find((item) => item.id === employeeId); const role = data.roles.find((item) => item.id === employee?.roleId); return role ? [...role.pathwayMappings].sort((a, b) => a.priority - b.priority).map((item) => item.pathwayTitle) : []; }
  function submit(event: FormEvent) {
    event.preventDefault(); if (!draft) return;
    if (!draft.employeeId || !draft.pathwayTitle.trim()) { setError("Employee and pathway are required."); return; }
    const anotherActive = data.applications.some((item) => item.employeeId === draft.employeeId && item.id !== draft.id && activeApplicationStatuses().includes(item.status));
    if (anotherActive && activeApplicationStatuses().includes(draft.status)) { setError("This employee already has an active application."); return; }
    saveApplication({ ...draft, pathwayTitle: draft.pathwayTitle.trim(), updatedAt: nowIso() }); setDraft(null); setError("");
  }

  return <MvpPanel title="Applications" eyebrow="Approval workflow"><MvpToolbar search={search} onSearch={setSearch} placeholder="Search employee, pathway or status" actionLabel="Create application" onAction={() => setDraft(blank())} filters={<select value={status} onChange={(event) => setStatus(event.target.value)} className="h-10 max-w-64 rounded-lg border border-[#102c3d]/[0.09] bg-white px-3 text-sm font-semibold"><option>All</option>{requestStatuses.map((item) => <option key={item}>{item}</option>)}</select>} />{visible.length ? <TableShell><TableHead><tr><th className="px-4 py-3">Employee</th><th className="px-4 py-3">Pathway</th><th className="px-4 py-3">Status</th><th className="px-4 py-3">Submitted</th><th className="px-4 py-3 text-right">Actions</th></tr></TableHead><TableBody>{visible.map((application) => <tr key={application.id}><td className="px-4 py-3 font-semibold">{displayEmployee(data.employees.find((item) => item.id === application.employeeId))}</td><td className="px-4 py-3 text-[#102c3d]/62">{application.pathwayTitle}</td><td className="px-4 py-3"><StatusBadge tone={statusTone(application.status)}>{application.status}</StatusBadge></td><td className="px-4 py-3 text-[#102c3d]/54">{application.submittedAt}</td><td className="px-4 py-3"><div className="flex justify-end gap-2"><TableAction onClick={() => setDraft({ ...application })}>Edit</TableAction>{["Submitted to Line Manager", "Awaiting Manager Review"].includes(application.status) ? <><TableAction onClick={() => updateApplicationStatus(application.id, "Approved by Line Manager")}>Approve</TableAction><TableAction onClick={() => updateApplicationStatus(application.id, "Declined by Line Manager")} danger>Decline</TableAction></> : null}{["Approved by Line Manager", "Submitted to Apprenticeship Lead", "Awaiting Final Approval"].includes(application.status) ? <><TableAction onClick={() => updateApplicationStatus(application.id, "Approved for Enrolment")}>Final approve</TableAction><TableAction onClick={() => updateApplicationStatus(application.id, "Declined by Apprenticeship Lead")} danger>Decline</TableAction></> : null}</div></td></tr>)}</TableBody></TableShell> : <EmptyState title="No applications yet" copy="Create an application from an employee record and track it through manager and final approval." actionLabel="Create application" onAction={() => setDraft(blank())} />}{draft ? <MvpModal title={data.applications.some((item) => item.id === draft.id) ? "Edit application" : "Create application"} onClose={() => setDraft(null)}><form onSubmit={submit}><FormGrid><FormSelect label="Employee" value={draft.employeeId} onChange={(value) => { const pathways = pathwaysForEmployee(value); setDraft({ ...draft, employeeId: value, pathwayTitle: pathways[0] ?? "" }); }} required options={[{ value: "", label: "Select employee" }, ...data.employees.filter((item) => item.status === "Active").map((item) => ({ value: item.id, label: item.name }))]} /><FormSelect label="Status" value={draft.status} onChange={(value) => setDraft({ ...draft, status: value as RequestStatus })} options={requestStatuses} /><FormField label="Pathway" value={draft.pathwayTitle} onChange={(value) => setDraft({ ...draft, pathwayTitle: value })} required /><FormField label="Submitted date" type="date" value={draft.submittedAt} onChange={(value) => setDraft({ ...draft, submittedAt: value })} /><FormTextArea label="Reason for interest" value={draft.reason} onChange={(value) => setDraft({ ...draft, reason: value })} wide /><FormTextArea label="Career goal" value={draft.careerGoal} onChange={(value) => setDraft({ ...draft, careerGoal: value })} wide /><FormTextArea label="Decision note" value={draft.managerNote} onChange={(value) => setDraft({ ...draft, managerNote: value })} wide /></FormGrid><FormActions onCancel={() => setDraft(null)} label="Save application" error={error} /></form></MvpModal> : null}</MvpPanel>;
}